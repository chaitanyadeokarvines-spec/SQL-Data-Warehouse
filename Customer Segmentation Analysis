SELECT 
    c.Segment,
    COUNT(DISTINCT f.Sales_ID) AS Number_Of_Transactions,
    AVG(f.Total_Revenue) AS Avg_Order_Value
FROM Fact_Sales f
JOIN Dim_Customer c ON f.Customer_ID = c.Customer_ID
GROUP BY c.Segment;
